const Page = ({ params }: { params: { slug: string } }) => {
  return (
    <div className="h-[400px] bg-zinc-900 text-white">
      <section>
        section1
      </section>
      <section>
        section2
      </section>
    </div>
  );
};

export default Page;